A city's skyline is the outer contour of the silhouette formed by all the buildings in that city when viewed from a distance. Given the locations and heights of all the buildings, return the skyline formed by these buildings collectively.

The geometric information of each building is given in the array buildings where buildings[i] = [lefti, righti, heighti]:

lefti is the x coordinate of the left edge of the ith building.
righti is the x coordinate of the right edge of the ith building.
heighti is the height of the ith building.
You may assume all buildings are perfect rectangles grounded on an absolutely flat surface at height 0.

The skyline should be represented as a list of "key points" sorted by their x-coordinate in the form [[x1,y1],[x2,y2],...]. Each key point is the left endpoint of some horizontal segment in the skyline except the last point in the list, which always has a y-coordinate 0 and is used to mark the skyline's termination where the rightmost building ends. Any ground between the leftmost and rightmost buildings should be part of the skyline's contour.

Note: There must be no consecutive horizontal lines of equal height in the output skyline. For instance, [...,[2 3],[4 5],[7 5],[11 5],[12 7],...] is not acceptable; the three lines of height 5 should be merged into one in the final output as such: [...,[2 3],[4 5],[12 7],...]
import java.util.*;

class Solution {
    public List<List<Integer>> getSkyline(int[][] buildings) {
        List<List<Integer>> result = new ArrayList<>();
        List<int[]> events = new ArrayList<>();
        
        // 1. Convert buildings into critical start and end events
        for (int[] b : buildings) {
            // Start event: use negative height to distinguish and sort properly
            events.add(new int[]{b[0], -b[2]});
            // End event: use positive height
            events.add(new int[]{b[1], b[2]});
        }
        
        // 2. Sort events by x-coordinate, using heights as tie-breakers
        Collections.sort(events, (a, b) -> {
            if (a[0] != b[0]) {
                return Integer.compare(a[0], b[0]);
            }
            return Integer.compare(a[1], b[1]);
        });
        
        // 3. TreeMap acts as a Max-Heap with efficient multi-set removals
        // Key: Height, Value: Frequency count of this height
        TreeMap<Integer, Integer> activeHeights = new TreeMap<>();
        activeHeights.put(0, 1); // Base ground level
        
        int prevMaxHeight = 0;
        
        // 4. Sweep across the timeline
        for (int[] event : events) {
            int x = event[0];
            int h = event[1];
            
            if (h < 0) {
                // Start event: add height to active map
                int positiveH = -h;
                activeHeights.put(positiveH, activeHeights.getOrDefault(positiveH, 0) + 1);
            } else {
                // End event: remove height from active map
                int count = activeHeights.get(h);
                if (count == 1) {
                    activeHeights.remove(h);
                } else {
                    activeHeights.put(h, count - 1);
                }
            }
            
            // Current highest active building
            int currentMaxHeight = activeHeights.lastKey();
            
            // 5. If maximum height changes, we found a key point
            if (currentMaxHeight != prevMaxHeight) {
                result.add(Arrays.asList(x, currentMaxHeight));
                prevMaxHeight = currentMaxHeight;
            }
        }
        
        return result;
    }
}
